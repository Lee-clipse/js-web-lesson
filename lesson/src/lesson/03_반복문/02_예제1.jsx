import React from "react";

const Index0302 = () => {
  const companyList = ["FEDEX", "UPS", "EMS", "K-PACKET"];

  return (
    <div>
      <div id="lesson-title">조건문 - 문제 1: 배송사 출력</div>
      <div id="lesson-content">
        <div className="title">문제</div>
        <div>`companyList`를 태그와 함께 출력하도록 반복문 코드를 작성하시오.</div>

        <div className="title">답</div>
        {/* TODO */}
        <div>{}</div>
      </div>
    </div>
  );
};

export default Index0302;

/*
  companyList.map((company) => {
    return <div>{company}</div>;
  })
*/
